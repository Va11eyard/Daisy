# A/B arm B: Qwen3-8B base + simple inference (minimal prompt, no LoRA/RAG/QC).
$schema: https://azuremlschemas.azureedge.net/latest/managedOnlineDeployment.schema.json
name: gpu-deployment-v14
endpoint_name: daisy-therapy
code_configuration:
  code: ../inference
  scoring_script: score.py
environment:
  conda_file: ../inference/conda.yaml
  image: mcr.microsoft.com/azureml/openmpi4.1.0-cuda11.8-cudnn8-ubuntu22.04
environment_variables:
  BASE_MODEL: "Qwen/Qwen3-8B"
  MODEL_DISPLAY_NAME: "daisy-model"
  INFERENCE_BUILD: "2026-06-qwen3-simple"
  INFERENCE_QUANTIZATION: "4bit"
  DEFER_MEMORY_UPDATE: "true"
  DAISY_DIRECT_MULTILINGUAL: "true"
  USE_AZURE_TRANSLATOR: "false"
  USE_OPENAI_TRANSLATION: "true"
  USE_AZURE_OPENAI: "true"
  DAISY_INFERENCE_MODE: "simple"
  DAISY_RAG: "false"
  DAISY_BM25: "false"
  DAISY_CONFIDENCE_GATE: "false"
  DAISY_DEFAULT_MAX_TOKENS: "384"
instance_type: Standard_NC4as_T4_v3
instance_count: 1
request_settings:
  request_timeout_ms: 180000
  max_concurrent_requests_per_instance: 1
liveness_probe:
  initial_delay: 600
  period: 60
  timeout: 30
readiness_probe:
  initial_delay: 600
  period: 60
  timeout: 30
